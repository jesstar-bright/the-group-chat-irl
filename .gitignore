# Private analytics page — keep out of the public site.
# Open it locally to view crew stats; never served to visitors.
dashboard.html

# OS cruft
.DS_Store

# Node tooling (tests only — the site itself ships no build)
node_modules/
test-results/
playwright-report/
